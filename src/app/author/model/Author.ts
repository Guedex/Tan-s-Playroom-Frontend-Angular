/**
 * Domain model representing an author.
 */
export class Author {
    /** Unique identifier. */
    id!: number;
    /** Full name. */
    name!: string;
    /** Author nationality. */
    nationality!: string;
}
